import image from '../images/crm.png';

export default function Index() {
    return (
        <div style={{ textAlign: 'center' }}>
            <img src={image} height={'65%'} width={'65%'} alt={'Online Bookstore'} />
        </div>
    )
};